---
surface: product
canonical_ui:
  mode: B
  source: runtime
  paths:
    - loading-ui.css
    - style.css
    - mystyle.css
    - quiz-help.css
    - mobile-experience.css
    - api/quiz-figure-image.mjs
    - android-webview-mode.js
    - android-app-theme.css
    - android-study-shell.css
    - android-study-shell.js
    - android-rotary-model.mjs
    - login-experience.css
    - login-experience.js
    - login-signs.mjs
    - greeting-view.mjs
    - src/daisyui.css
    - assets/daisyui.css
    - src/learning-insights.css
    - assets/fonts/magicbook-bangla-fonts.css
    - study-quiz.css
    - libreria-font.css
    - magic-styles.css
  notes: Existing application tokens remain canonical. Locally compiled, d-prefixed daisyUI controls provide interaction primitives; the scoped learning stylesheet owns layout and visual hierarchy.
design_context:
  owner: Magic Book
  last_updated: 2026-09-12
  revision_notes: "Owner-approved unibody login replaces the glass pass, brand heading and greeting rail on web and Android. Intact floating book, verified public traffic signs with dictionary Bangla, direct phone form, small signature and keyboard-aware document scrolling. Authentication and other routes unchanged."
---

# Magic Book design context

## Product intent

Magic Book helps adult, primarily Bangla-speaking learners in Italy decide what to study next for the driving-licence exam. Navigation and actions use short A2/B1 Italian. Bangla is supporting content where the catalog already supplies it. The interface reports observed quiz results only; it never predicts exam readiness or invents causes.

## Visual direction

- Direction: Adaptive Study Workspace — a calm, adult study tool that changes density with the amount of real data.
- Signature composition: recent-result band, compact action rail, five-by-five chapter matrix, and recovery stack.
- Tone: direct, professional, reassuring, and operational. Avoid poetic, analytical, and internal-system language in student-facing copy.
- Density: useful information in the first viewport, a restrained 1240px reading canvas on desktop, and one natural vertical flow on mobile.
- Surface rule: one bordered surface per section; use dividers for rows and tabs instead of cards nested inside cards. Elevation is reserved for the single next-action panel.
- Imagery: reuse `icons/statistiche-patente.png`, `icons/errori-patente.png`, `icons/go-back.png`, `icons/next.png`, `assets/admin/update.png`, and authenticated figure assets. Existing SVGs are reused only when their meaning matches.
- Icon rule: no Unicode characters as UI icons. Use repository assets, text labels, or decorative CSS marks hidden from assistive technology.
- Avoid giant headings, oversized empty surfaces, decorative gradients, neon, game styling, generic KPI-card grids, traffic-sign decoration, and horizontal scrollers.
- Android WebView compactness is owned by `mobile-experience.css`; it may reduce decoration and spacing without changing route meaning or hiding required controls. Header titles stay visually centered through symmetric live utility rails, and short Home content uses auto margins that collapse safely when vertical scrolling is needed.
- Installed-app color is owned by `android-app-theme.css` behind the `html.android-webview` marker. Its primitive palette is additionally exposed on `#login` for the explicitly approved shared web/Android login, without exposing native route aliases or styling. Pearl chrome, graphite controls and blue selection/actions follow the owner's latest palette reference; white reading paper stays distinct. Other browser screens keep their existing colors and geometry. Thin section-only Android headers are an explicit shared theme variant; business behavior and route structure remain unchanged.

## Tokens

- Brand action: `--li-action: var(--color-primary)`; dark action `#263bd4`.
- Android installed palette: primary/action `#076AE0`, strong/focus `#0755B3`, secondary/ink `#1D1D1F`, accent `#007AFF`, background/surface `#F5F5F7`, neutral decoration `#AAAAAA`, muted readable text `#69696E`, structural border `#76767A`, decorative divider `#D8D8DC`, and reading paper `#FFFFFF`. Primary and secondary controls use white labels; light surfaces use graphite. The reference's brighter blue is decorative; the existing darker action blue preserves normal-text contrast. Neutral decoration is never supporting copy.
- Token mapping (model B): `android-app-theme.css` owns `--app-palette-*` on native root, shared `#login` and shared live `body.quiz-focus`, maps native-only route aliases and supplies native theme-color metadata through `android-webview-mode.js`. `login-experience.css` and native study tokens consume this owner without duplicating palette values. The `aura-fluid` dataset remains a compatibility switch, not the palette source. The shared login and documented Android header/rotary variants are the explicit density, typography and motion exceptions.
- Semantic colors are independent of the installed palette: correct/positive remains green, incorrect/error remains red, and warning/limited data remains amber.
- Ink/navy: `#17233a` / `#12315f`.
- Positive: `--li-teal: #138f86`; attention: `--li-coral: #c84f4b`; limited data: `--li-amber: #a96c16`.
- Page/surface/line: `#f3f6fb`, `#ffffff`, `#d9e2ee`.
- Type: Inter/system sans for Italian UI. The large study-mode Bengali hero title uses self-hosted Hadi Rounded 400/700. All smaller Bengali study and quiz text—including the last-chapter subtitle, translations, controls, keyword chips, and word details—uses self-hosted Adorsho Lipi 400. The dictionary is the approved catalog exception: every element marked `lang="bn"` uses Tiro Bangla, with Noto Sans Bengali and Hind Siliguri as fallbacks. Primary Bangla vocabulary labels synthesize weight 700 from the provider's Regular 400 face; supporting explanations remain 400. Full translations use a fluid 1.15–1.2rem support scale; compact Bangla controls, keyword lines, and word details use .86–.92rem. Ekushey Lal Sabuj 400 remains locally available as an alternate and specimen in the font library.
- Italian display case: `italian-display.js` is the shared presentation owner. Chapter and topic tags are uppercase; ordinary Italian words, locutions, and explanatory copy start with an uppercase letter while preserving the remaining source spelling and intentional special forms.
- Radius: 16px learning cards, 8px controls, and 9999px status/action pills.
- Focus: 3px cobalt outline with 3px offset.
- Learning-route UI contract: Inter for display and body copy; JetBrains Mono or
  an equivalent system mono for technical labels and metadata. Cards use a
  16px radius, controls 8px, and status/action pills 9999px. Eccellente uses
  teal, Buono uses a distinct green, In miglioramento amber, and Da ripassare
  coral so status can be understood at a glance.

## Layout

- Mobile-first full-screen workspace with one sticky header and one sticky two-route switcher.
- Main width: up to 1720px with fluid side gutters. Desktop uses a 12-column relationship: Statistics overview about eight columns and immediate action about four; Errori explorer about nine and plan about three.
- At 1024px, chapter detail becomes sequential and the recovery plan becomes a compact horizontal region.
- At 767px and below, content becomes one vertical flow. In Errori, `Il tuo ripasso` precedes category exploration.
- The chapter matrix uses five columns when space permits and two columns on phones. All 25 chapters remain visible without horizontal scrolling.
- Reading order remains meaningful without CSS. Nothing may create page-level horizontal scrolling at 320px.
- Public access entry: Promo Code is temporarily disabled by the fail-closed `PROMO_LOGIN_ENABLED` switch in `script.js`; logged-out visitors go directly to the personal phone login and no promo-status request runs. When the switch is restored, the preserved promo landing uses a wide two-column pass from 1024px and keeps its vertical mobile flow.

## Components and behavior

- Browser Home OPEN gesture: the existing green button, image, label and route remain unchanged. A decorative34px hand uses the original `icons/clich_here.svg` and the shared `native-book-tap` timeline in `android-study-shell.css`. The named browser variant in `style.css` repeats every2.8s, with shorter14px-to-minus18px travel at the lower-right of OPEN; the Android book retains its5s cadence and original geometry. It rises black, becomes white at contact, compresses and fades without reflow. Hover, keyboard focus, activation, busy/disabled and route transitions hide it; reduced-motion, Profile pause and forced colors suppress it. The cue has no events, focus or spoken label; asset failure never blocks OPEN.
- Shared component base: locally compiled daisyUI 5 with the `d-` prefix. Header, route, primary, secondary, category, and pagination controls use these primitives; the scoped stylesheet remains the visual owner.
- Shared loading indicator: `loading-ui.css` is the canonical visual owner and `icons/loading.gif` is the approved animated asset. Page, panel, inline-status, and busy-control variants reserve stable geometry, retain Italian status text and `aria-busy`, block duplicate actions, and replace animation with a static high-contrast mark when reduced motion is requested. Background refresh keeps usable content visible instead of opening a blocking loader.
- Audio focus: `audio-focus.js` coordinates every Quiz and Studia quiz audio action. Explanation audio is resumable: question, keyword, and dictionary TTS pause it at the current timestamp and expose a violet dashed interrupted state. Only the latest TTS may play; natural completion resumes the same explanation, while manual stop, navigation, or an explicit explanation action suppresses automatic resume. Pending explanation requests are invalidated so a late response cannot overlap newer audio.
- Dictionary pronunciation (web and installed Android): `magic-dictionary.js` / `.css` add the supplied unchanged `icons/human_talking.png` in a44px right-hand control beside each Italian/Bangla section. A fixed icon column protects long copy; original card layout, fonts and separate platform presentation remain. One catalog-bound authenticated `getDictionaryAudio` operation on `api/quiz.js` selects the existing Italian or Bengali server TTS, never a device-default voice for Bangla. No new provider or native permission. Audio needs Internet; only12 recent clips are held in account/device-scoped session memory. Shared audio focus prevents overlap; repeat tap stops, natural end resumes interrupted explanations, search/filter/navigation/background cancels pending and active speech. Loading, playing, asset fallback, timeout, retry and permission-denied states retain the44px target, keyboard name and visible focus. An autoplay-blocked response stays cached for an explicit second tap. Errors appear beside the affected entry; no new overlay or automatic translation.
- Quiz blocking load: `mystyle.css` owns a single white viewport surface. The supplied GIF sits directly in the center without a visible card, border, shadow, progress copy, or nested loading surface; changing status text remains screen-reader-only. Recoverable failure appears only after loading ends and uses plain learner-facing Italian.
- WhatsApp group dialog: `style.css` owns one responsive white modal with a restrained green accent, local Hadi Rounded title and Adorsho Lipi support copy in Bangla, an aligned Bangla/Italian control, and two Admin-derived icon-led pill actions. It traps focus, supports Escape/backdrop dismissal, restores focus, and keeps the background inert while open.
- Admin identity: `assets/admin/ADMIN_PROFILE_LOGO.jpg` is the single Admin profile image. It fills the circular entry control and the compact circular identity mark beside the Admin title; both keep stable geometry and use an empty alternative because the surrounding control or title already supplies the accessible name. The floating Admin and Profile controls use solid white surfaces with an opaque line and no glass transparency or backdrop blur.
- Admin utility actions: `Nuovo utente`, `Aggiungi spiegazioni audio`, and `Magic Styles` share one compact icon-led pill component. Emerald, coral, and violet distinguish the three destinations without replacing their text labels; links remain links and the create action remains a button.
- Explanation audio player: `audio-player-ui.css` is the shared visual owner for Quiz, Studia quiz, and Admin explanation players (including legacy review). It carries the Admin search-pill language into one fully transparent surface with an emerald line and glow: a 40px emerald radial control using only the approved outlined play/pause SVG pair, emerald progress, violet speed utility, stable loading/error geometry, cobalt focus, and motion-safe hover/playing feedback. All three surfaces use the same emerald control, transparent surface, violet speed selector, and supplied SVG pair. Both SVGs share the same grid cell so playback state never shifts the control; route-level pseudo-icons and black active backgrounds are explicitly retired. The speed selector starts at 1x, moves to 0.8x on its first activation, then cycles through 1x, 1.25x, 1.5x, and 2x. A bounded 56px speed column, reduced to 52px on narrow phones, keeps `1,25×` and every other label inside the player while the progress track uses the remaining flexible width. The approved `quiz-focus` live-Quiz variant (installed Android only) is the compact exception: 44px total height, no adjacent sticker, a flat blue play control, a slim progress track and 48px speed column. Result-row artwork and Study/Admin player geometry are unchanged.
- App transition ownership: `script.js` exposes one action gate for Home, chapter, Exam, Quiz-mode, Admin, and delayed route changes. The first accepted action owns the transition until release; competing taps are ignored, browser history cancels stale delayed work, and the active native shell temporarily removes pointer interaction from conflicting launch controls without shifting their geometry.
- Legacy Aura chapter interaction: the retained carousel follows the pointer, resists edges and projects release by at most six chapter steps. Cards remain roving-tabindex buttons with tap/keyboard alternatives. The installed Home/chapter route now uses the rotary owner documented below; this legacy carousel is not a second visible selector there.
- Admin search and filters: the phone field is an emerald icon-led pill with no visible placeholder, while retaining a programmatic Italian name and explicit clear control. Users, Promo, In scadenza, Scaduti, and Duplicati use the same pill radius with green, violet, amber, red, and slate state accents; runtime values are owned by `style.css` under `.admin-toolbar` and consumed by the search, tabs, and utility actions.
- Admin user dataset: entry loads at most the 10 newest registrations. A labelled scope bar explains whether the current data is recent, searched, or complete; exact phone lookup stays remote, while `Carica tutti gli utenti` is the explicit opt-in for full-list filters such as Promo and Duplicati.
- Live quiz bilingual help: `quiz-help.js` remains the sole state/data owner. The owner-approved phone variant portals the same workspace to the document body as a full-viewport white reader, with a compact persistent Traduzione/X rail and one content scroller. The Italian source question comes first; a real figure, when present, sits beside it in an80–112px contained frame. The existing protected figure node is moved and restored, never cloned or separately fetched; no-figure questions omit the brand placeholder in the reader. Translation, expanded word meaning/audio, keyword pills and optional context follow in that order. Phone means width≤600px, or coarse-pointer width≤950px/height≤500px for landscape; larger/tablet screens retain the inline glass panel in the question scroller. This explicitly supersedes the earlier no-modal rule only for phones. The shared `magic-styles.css` surface and18px X/44px target remain unchanged inline. Closing, navigation and question changes invalidate pending help/audio; opening is always explicit.
- Live quiz question footer: figure, Italian question, and optional Admin recorder keep their semantic source order in the card’s dedicated scroll region. “Spiega” and the first-question help hint share a reserved, wrapping footer below it; neither uses overlay coordinates. The footer remains visible while long text scrolls independently and keeps important controls at least 44px tall.
- Quiz figure presentation: `api/quiz-figure-image.mjs`, reached through `api/asset.js`, is the single owner for every catalog figure shown in Quiz, quiz correction, Studia quiz, Statistiche/Errori, and Admin explanation tools. It removes only isolated, neutral digit-sized components on the upper-left label line, never a fixed rectangle. Lossless PNG output preserves the other decoded pixels; an uncertain label leaves the source figure intact. This runs before the image reaches CSS; therefore resizing, browser zoom, text zoom, orientation, and `object-fit` cannot reveal it. Original R2 objects and separate Admin metadata remain unchanged. Transformation failure is fail-closed and shows the owning surface's existing image fallback rather than an unprocessed figure.
- Admin quiz answer marker: only a server-authorized Admin session receives the private answer value. One small green marker sits below `Vero` when true is correct, or one small red marker sits below `Falso` when false is correct; ordinary learners receive neither the value nor reserved marker space, and assistive technology receives an explicit Admin-only text equivalent.
- Quiz timer continuation: the timer counts down through the normal duration for every role. At zero, a server-authorized Admin session changes to a `+0:00` elapsed-overtime display and continues until the Admin finishes or exits. A non-Admin session pauses at `0:00` and opens the shared timeout dialog; `Sì, continua` starts a fresh full-duration cycle while preserving total elapsed time, and `Chiudi quiz` returns to the menu without grading. The decision repeats after every completed cycle. Result summaries report the complete elapsed time.
- Quiz correction audio: every correction row—correct, wrong, or unanswered—keeps the existing person artwork as a labelled audio button. Audio remains demand-loaded after activation, one review explanation plays at a time, and unavailable audio returns the existing recoverable message.
- Magic Styles: `/libreria-font` remains the stable Admin-linked reference route, now labelled `Magic Styles`. Its live component collection preserves the Exam 80 glow card, chapter card, progress bar and shared loading indicator. Demonstration progress is explicitly labelled as an example; cards link to their real destinations. `magic-styles.css` owns the shared card/progress rules and the approved Exam 80 accent: a white reading surface, a top-right violet sparkle using `icons/magic-sparkles.svg`, and a restrained animated perimeter blending lilac `#c4b5fd`, rose `#fbcfe8` and mint `#a7f3d0` over nine seconds. Reduced motion keeps a static glow, forced colors retain a visible border, and keyboard focus remains explicit. This requested accent is reserved for highlighted actions rather than every card. The font specimen sheet contains three divided rows—Hadi Rounded, Adorsho Lipi, and Ekushey Lal Sabuj—and renders the same live Bengali sentence in each face for an honest comparison without image previews.
- Global offline notice: every application page loads the same blocking connection-loss alert. It uses one calm white Magic Book surface, the existing no-internet illustration, a textual waiting state, focus containment, and automatic recovery without a dismiss action while the browser remains offline.
- Learning shell: back, brand, labelled/icon-only responsive refresh, Statistics/Errori route switcher, freshness note, and one polite live status. Legacy fixed chrome is suppressed while this route owns the viewport.
- Statistics overview: answers correct, quizzes done, chapters started, items to review, and the real percentage from the recent window. Comparison copy is plain and threshold-based.
- Immediate action: exactly one useful action in the first viewport. Empty and insufficient histories get a quiz action; ready users get the first real plan action or a positive continue state.
- Progress groups: at most three chapters going well and at most three chapters to review; no duplicate metric wall.
- Chapter matrix: 25 compact cells with number, clamped title, explicit status, and optional review count. Status vocabulary is `Bene`, `Sta migliorando`, `Da ripassare`, `Pochi dati`, `Non iniziato`.
- Chapter detail: one selected chapter at a time, side-by-side on wide screens, sequential on tablet, and a focusable bottom sheet on phones; it shows attempts, quiz coverage, correct answers, recent result, review/recovered counts, and existing quiz/book actions.
- Error summary: `Da ripassare`, `Sta migliorando`, and `Recuperati` in one grouped strip.
- Error categories: five stable tabs — Figure, Quiz, Parole, Argomenti, Capitoli. Recovered items are a separate positive section, not a duplicated tab.
- Error master/detail: figures show the real authenticated numberless presentation; words retain dictionary access; detail copy states observed counts without causal diagnosis. Lists start at eight items and use `Mostra altri`.
- Review plan: one to three actions, total estimated time, and immediate CTA using the existing quiz, book, figure, or dictionary destinations.
- Recovered: up to three recent recovered items remain visible below active work.

## Data, states, and trust

- Admin authentication and device-bound authorization remain unchanged. Its read flow uses a bounded recent-list request, remote phone search, and an explicit complete-list request; the server still validates and normalizes phone data before forwarding it upstream.
- Empty, insufficient, ready, cached, refreshing, offline-cached, no-cache offline, expired access, timeout, and generic failure are distinct states.
- Empty/insufficient views stay compact and tell the learner exactly which quiz action is available.
- Cached data renders first; online data replaces it only after validation. Pending local responses remain identified without blocking the UI.
- Figures are lazy loaded, use a stable aspect frame, receive their numberless pixels from the shared asset endpoint, and expose a visible fallback without layout shift.
- Backend diagnostic labels and reasons are translated into simpler presentation copy; the underlying classification and calculations are untouched.

## Accessibility and validation

- Route entry focuses `#learningInsightsHeading`; background refresh does not steal focus. Re-rendered tabs, chapter cells, disclosure controls, and close actions restore focus.
- Tabs implement Left/Right/Home/End. Selected and expanded controls expose matching ARIA state/control relationships.
- Status is expressed with text and color. Interactive targets are at least 40–44px, focus is visible, and reduced-motion/forced-colors modes are supported.
- Long quiz questions and reader-size variants reflow before their action footer; no question utility may cover copy at 320px, browser zoom, or iOS text scaling.
- Verify 320, 375, 430, 768, 1024, 1280, 1440, and 1920 widths; capture 375, 768, 1440, and 1920 evidence.
- Verify empty, 1–9 answers, medium history, large history, figure failure, offline cache, expired access, timeout, and generic backend failure.
- No Unicode UI icons, fabricated sequences/charts, inferred causes, duplicate global chrome, or hidden horizontal overflow patches.
## Installed Android rotary study variant

- Scope: Home and chapter templates mount only in `html.android-webview`. Desktop/mobile browsers and installed browser PWAs never mount them. Quiz/Exam mode selection also uses the native full-page variant below; other Android routes receive the shared palette and thinner section-only header, not a new route layout.
- Shared login (web and Android): `login-experience.css` owns the approved unibody presentation and the shared Norwester font-face; `login-experience.js` owns presentation lifecycle/keyboard visibility, and `login-signs.mjs` owns a tiny curated catalog. This explicitly replaces the previous glass pass, two-line brand and cycling greeting. One pearl-white canvas mixes canonical background65% with paper; no raised card, blur, border or duplicate brand. Intact `icons/mg_book.svg` stays contained without added filter/shadow. Next to it, three existing numberless signs (fig40, fig41, fig218) carry unchanged short dictionary meanings in Hadi Rounded, with Italian accessible names. The first pedestrian synonym is used, not invented copy. This educational sign treatment is an explicit login exception to the no-decorative-traffic-sign rule. Fixed media/caption geometry prevents form movement; narrow layouts use a two-part top area followed directly by the phone form, while900px+ uses artwork left/form right on a1060px canvas. Inputs retain labelled60px targets, Continua58px, coherent18px corners and canonical blue/ink/muted/border roles. Validation/error tones, phone normalization, password manager/paste, admin challenge, device binding, exclusive submit and routes remain untouched.
- Shared login motion and resilience: the original book floats from+3px to-8px with ±1.2-degree rotation over5.6s. Each decoded sign rests4.6s, exits320ms and enters620ms with one of four restrained transforms (±3degrees, ≤12px). Timers, image listeners/timeouts and animations cancel on navigation, background or form focus; stale decodes cannot replace the current sign. A small44px pause/resume control is available outside the form; system/Profile reduced motion keeps content static. Only the three public image URLs are requested on demand, never the full dictionary or auth data. Failed signs are skipped; all-image failure keeps the real short meaning in its reserved frame and never blocks access. Online/re-entry retries are bounded. The document retains natural scrolling, dvh/svh fallback and safe-area padding. VisualViewport resize keeps focused input/Continua visible with extra bottom space only when a keyboard overlays the layout viewport; pinch zoom is not overridden. Physical Android keyboard behavior still requires device verification. Security copy and the original512×137 signature share a compact footer:132px monochrome CSS rendering, no black container, unchanged link and no edits to the PNG. Other signatures and native Home greetings are unchanged.
- Chapter stage offset: above620px viewport height, move `.native-orbit-stage` up14px as one unit, including the wheel, Vai and title/card. Header, six actions and dock remain fixed in their existing positions. Compact/landscape views retain the original stage placement and header clearance. Stage sizing and title changes cannot move neighboring controls.
- Evidence: owner-supplied 2026-09-08 mockups and follow-ups. The latest written request overrides the screenshot: transparent wheel on the right, complete square-cornered card on the left. The pearl/blue palette, supplied traffic divider, soft book motion and liquid progress remain approved. Other Android routes are unchanged in this revision.
- Runtime owners: `android-study-shell.css` owns visual tokens/layout; `android-study-shell.js` owns DOM/lifecycle; `android-rotary-model.mjs` owns bounded direct manipulation. `script.js` still owns routes, selected chapter, the exclusive action gate and authorization. No second navigation or permission model.
- Composition: pearl Home, fixed 44px greeting viewport, original `icons/mg_book.svg`, no CSS shadow/filter. Book opens chapter selection. A transparent numbered arc is anchored beyond the right edge. Its thin divider uses the decorative divider token; the selected 01–25 value and its small dot are blue. The dot shares the selected number's angle exactly on the 100-unit circumference, including during drag and settling. A fixed 40%-wide frame at left presents title, then complete card. Solid-blue Vai with white text sits inside the empty semicircle, in a56–72px square target with an18px label and the shared soft scalloped silhouette, without an outer ring. Only its decorative background is masked; the semantic button, centered label and keyboard focus stay unclipped. A blue label core preserves white-text readability if the decorative asset fails. Forced colors use Highlight/HighlightText; system/profile motion preferences disable press scaling. Its center and surrounding geometry are unchanged. It is a sibling of the slider, never a nested interactive slider child. There is no black wheel, duplicate readout or number badge. Six actions and the traffic divider remain above the dock.
- Dock: black pill, real quiz-coverage liquid track on the left, existing Profile and authorized Admin controls on the right (the explicit written placement takes precedence over the mockup). Original elements are moved, not copied; event handlers and permissions are preserved. Profile remains the existing phone/logout popover, with the app-only animation preference.
- Tokens (model B): native canvas/ink/dock/blue/muted/line alias app background/text/secondary/primary/muted/border. The wheel has no fill. Liquid body `#9CC6FF` and wave tints `#CEE2FF`/`#E2EEFF` remain supporting progress roles. Existing semantic error/success colors are independent.
- Type: self-hosted, unmodified Norwester v1.2 for short Latin display/action labels; self-hosted El Messiri variable 500 for ornamental chapter titles, with Georgia/serif fallback. Six bilingual actions use existing Adorsho Lipi 400 via `--font-bn-support`, 14–18px, without uppercase or letter spacing. Latin names remain unchanged. Font licenses accompany their assets; no runtime third-party font service. Arial/system remains smaller explanatory copy.
- Selected digits smoothly size from 22 to 32 SVG units according to distance from the central detent. Color, size and the slider's full chapter value jointly identify the selection. Forced colors uses CanvasText/Highlight. No second inner value exists.
- Behavior: one captured pointer, 33-degree detents with a 0.08-step midpoint deadband, no inertia or wrapping, clamp 01–25. Numbers ascend top-to-bottom and follow the finger; upward dragging selects the next chapter. Release/keyboard selection settles over 220ms ease-out. Tapping a number and Arrow/Home/End/Page keys remain non-drag alternatives; movement over 6px cannot become a tap. Drag release/cancel never launches. Resize or navigation releases capture and cancels stale animation. The canonical action gate rejects simultaneous routes and Back restores selection.
- Haptics: Android `MagicBookHaptics` uses official AndroidX Core 1.16 `ViewCompat.performHapticFeedback`: one `SEGMENT_TICK` per selected chapter, two `KEYBOARD_TAP` events 85ms apart at an attempted boundary. The boundary replaces any simultaneous selection tick, never three pulses. Only exact trusted HTTPS main-frame selection/boundary messages are accepted. System settings/hardware are respected; navigation cancels pending feedback. No arbitrary duration or VIBRATE permission. Browser vibration is best-effort fallback. Native changes require an updated installed shell; physical feel remains device QA.
- Motion: 5s gentle book float and restrained liquid wave. Home greets once with `আসসালামু আলাইকুম` in Hadi Rounded (`--font-bn-title`, lang=bn), the phone-local-hour greeting (05:00–11:59 Buongiorno, 12:00–17:59 Buon pomeriggio, otherwise Buonasera), then `Ciao!` in Norwester, in a fixed44px /9s upward rail. The shared login instead uses the owner-approved sign composition documented above. The supplied hand repeats every5s, starts black below the book, becomes white over the artwork, compresses and fades. Bent-arrow cues move along mirrored24–40-degree arcs every3.8s, lower cue delayed .45s, outside the numbered orbit; they fade during interaction and return after4.5s idle. Decorative cues never intercept input. Profile motion preference and system reduced-motion remove motion; background/navigation cancels transient work. No instructional copy or added book shadow.
- Progress definition: `summary.quizCoveragePct` against `dataQuality.catalogQuizCount` from the existing authenticated learning-insights model. It means unique quiz questions encountered, not chapters read, mastery or exam readiness. Unknown/error is an em dash, genuine zero is 0%, saved data is marked. Requests are user-scoped, abortable and stale-protected; no fake fill while loading.
- Chapter artwork: the existing manifest maps all25 topics to optimized 640px-wide All Books covers; originals are unchanged. Only the selected image loads after120ms idle. The title stays for at least1.8s, then decoded artwork replaces it with a260–300ms fade. Missing/slow images keep the real title. Selection, drag, hiding and navigation invalidate pending reveals. The accessible title stays available with decorative artwork; explicit pause keeps the title. The full card, including its original artwork footer, uses bounded intrinsic dimensions, `object-fit: contain` and square corners (`border-radius: 0`). No crop or image semicircle. Provenance: `assets/chapter-covers/README.md`.
- Layout: max540px canvas; only the off-edge circular arc is intentionally cropped. The sole scroller ends12px above the measured dock. Selection and actions share one bounded workspace with auto margins that collapse on short screens. Its minimum is420px (398px below620px); its maximum is the capped dial height plus210px for actions (188px compact). The stage keeps a210px minimum and the dial is bounded by min(78vw,421px). This groups the controls without stretching empty space between them. Title/artwork share one fixed frame; El Messiri fits locally from28px to16px, remeasured after font readiness. Enlarged titles may scroll within that frame but never move surrounding geometry. Vai follows wheel geometry and fits completely inside its line. All six controls share48px height, compact44px below620px; landscape scrolls above the dock. Normal browser geometry is unchanged.
- Bilingual actions: Study Quiz → কুইজ পড়ুন; Do Quiz → কুইজ করুন; Dictionary → শব্দার্থ; Exam → কঠিন প্রশ্নগুলো; Statistiche → আমার অগ্রগতি; Errori → ভুলগুলো. Each canonical button contains a fixed40px clipped rail with original/Bangla/original rows. A9.2s cycle holds original2.76s, slides upward for.552s, holds Bangla3.312s, then slides upward to original. Targets never resize. Stable accessible names contain both language-tagged labels; animated duplicates are aria-hidden. Focus/hover pauses reading; background/navigation pauses motion. Profile pause or reduced motion displays both labels statically in two20px rows.
- Roundabout: only the supplied blue SVG rotates, not the road lines. A40s eased pattern alternates one clockwise, one counterclockwise, two counterclockwise and one clockwise turn, with varied quiet intervals. Single turns last2s, double turns4s; full-turn endpoints avoid visible resets. The divider is quieter at22px, with original road patterns at45% opacity. Profile/system motion preferences remove animation; background/navigation pauses it.
- Action icons: use the owner's original `study_quiz.svg`, `do_quiz.svg`, `dictionary.svg`, `exam.svg`, `Statistics.png` and `errors.png` from `icons/`. The four monochrome SVGs render white alongside their labels on graphite/blue buttons; PNG artwork keeps its original colors. Originals are not edited. A fixed30px solid scalloped icon slot uses20px SVGs and22px PNGs. The icon and reserved label rail are centered as one group, with8px between their slots; both moving language rows are centered in the rail. The action group uses min(92%,392px),12px column gaps,8px internal side padding and a14–18px Latin scale; both languages remain readable at320px. Decorative, non-draggable images have empty alternatives, reserve their dimensions and are cached offline. No new action handlers or browser UI changes.
- Chapter title fitting: wrap only between complete words, without hyphenation or emergency character breaks. A zero-minimum grid track makes actual word overflow measurable; the existing28–16px local fitter checks width and height, with cached measurements keyed by frame, title and font family and invalidated after font readiness. The fixed title/artwork frame and all surrounding controls stay still. Full long content remains accessible and scrollable at the minimum size; never clip, abbreviate or hide trailing letters.
- Native matte action buttons: the six chapter actions use one solid28px scalloped backing in a30px icon slot, with no translucent front, blur, reflection, gradient, shadow or raised edge. Graphite buttons use a blue backing, blue buttons a graphite backing; originals stay20px white SVGs and22px color PNGs. The shared cached silhouette and Vai remain unchanged. The30px icon column and maximum intrinsic bilingual label column are centered together with an8px gap; each language line is centered within its fixed40px viewport. Both languages determine the reserved width, so switching language never moves the icon or resizes the button. Labels,48px/compact44px targets and13px button corners remain. Hover darkens to95%, press to88%; the icon compresses to94% over160ms without moving the target or label. System/Profile motion preferences suppress scaling; forced colors preserve the dark scallop and system button border. The existing passive contact tracker still clears on movement beyond6px, release/cancel, blur, resize, modal/route entry and hidden state; it never captures, prevents scrolling or dispatches actions. Canonical routes, permissions, headers, artwork and normal-browser UI are unchanged.
- Shared Android chrome: Home/chapter title bars retain one section name; reader header is48px, study/library/dictionary/learning/Admin bars are approximately52px minimum with44px interactive targets. Branding subtitles are hidden only in native headers; Statistics/Errori use their actual section name. No permissions or navigation actions are hidden by this density change.
- Section icons: Android Study, Dictionary, Quiz and Exam headers reuse the corresponding original action SVG at20px beside the section title, with an8px gap. Empty CSS masks inherit the readable heading color: graphite on pearl chrome, not white on a light surface. Statistics/Errori reuse the original-color PNGs in the existing header and two-route switcher slots; no duplicate icons. Quiz and Exam mode sheets use the same mapping, and the Exam PDF header reuses its existing image slot. Quiz chrome reserves independent exit/title/tools columns so the symbol cannot overlap the timer or text controls. Existing accessible headings, thin bars, navigation and normal-browser presentation remain unchanged. Exception: the installed Android `quiz-focus` live Quiz/Exam header deliberately omits its symbol; only its section title and compact grouped timer/reader controls remain.
- Card activation: the full-image frame is a native button using the same selected-chapter action as Vai. Its current chapter/title supplies the accessible name; the image itself stays decorative and non-draggable. Title/loading/failed/stale artwork is disabled, hidden from the accessibility tree and cannot intercept input. Only the current decoded image enables the button. No added border, background, rounding or shadow; keyboard focus is explicit, hover dims subtly and press gently compresses with reduced-motion support. Leaving/changing the selection invalidates the image action immediately.

- Rotary recovery: one primary pointer owns selection, using capture when available and window-level move/up/cancel as a fallback. Finalization clears ownership before releasing capture, tolerates unavailable capture, runs on blur/modal entry and keeps the last selected chapter. A fresh primary sequence of the same device type replaces a stale unfinished sequence; secondary fingers and other device types cannot steal an active gesture. The already-visible chapter screen accepts local selection during its280ms entry gate, but all book/section launches still use the unchanged exclusive navigation gate. Existing geometry, bounds, detents and haptic policy remain unchanged.

### Native Quiz and Exam mode screens

- Owner: `android-mode-screens.js` / `.css` adapt the existing `script.js` mode selectors; no second quiz, auth, trial or navigation engine. Only the installed Android marker mounts this named presentation variant. Ordinary browsers keep the original sheets and Study entry. Turning off these two presentation assets restores the legacy controls.
- Quiz becomes `/scegli-quiz`, Exam `/scegli-exam`: full opaque pearl pages, no scrim, sheet grip or floating dock. Both retain a symmetric compact section-only header. Quiz places Mix, Capitolo, Multi in a thumb-zone footer immediately above Inizia; Exam keeps its 80 quiz, 30 quiz, PDF switcher below the header. The redundant Study card is hidden only here; the dedicated native Study action remains. These selector headers omit the old symbol prefix; actual quiz/reader headers retain their approved icons.
- Use existing runtime palette roles (canvas, paper, text, muted, divider, primary and primary-strong); do not create a second blue or change feedback semantics. Surfaces are matte, with no glass, ornamental frame or shadow. Quiz has one content scroller above a normal-flow, non-shrinking footer: mode tabs and the original active Start node remain visible, including on short/landscape screens. Exam retains its content-end Start. No underlay is focusable through the native route.
- Quiz mode explanations live at the top, alternating Italian and Bangla on the same9.2s cadence as chapter actions (first change after2.76s, soft552ms transitions). Overlapping grid rows reserve the larger translation's natural height; no fixed clipping or word fragmentation. Use Norwester Italian headings, HadiRounded Bangla headings and AdorshoLipi Bangla supporting copy. Permission notes and selection summaries remain stable, outside the animated copy. Both languages have static accessible text; system reduced-motion/Profile pause shows both without animation. No new layer or color variant.
- Mix's real30-question count sits higher, optically centered in the existing white arch, with a bilingual caption below. Its two unchanged Norwester digits drift gently by3px on a5.6s cycle; this is decoration, not an interactive control or changing count. Copy/count motion pauses on background, transitions and launch. The footer geometry does not change between modes or language frames; original IDs, listeners, disabled rules and guest attempt labels remain authoritative.
- Chapter controls use the supplied arch silhouette at rest and a blue soft four-lobed silhouette for selection, with Norwester tabular digits. The same 5-column geometry holds all 25 chapters in Capitolo/Multi; targets are at least44px. The decorative selected mask has a readable blue-core fallback. `aria-pressed` plus a text selection summary supplement color. Multi retains the canonical minimum of2 chapters, with no new eligibility rules.
- Route entry focuses the heading. Tabs use semantic tab/tabpanel relationships, roving focus and Left/Right/Home/End. Back/Escape, system history and direct reload use the canonical router; direct reload returns safely to chapters. A pending launch disables controls without changing their geometry, keeps the current page until the existing destination opens, and is cancelled/restored on Back. No duplicate launch handlers.
- Motion: a restrained240ms page entry and160ms selection/press feedback; reduced-motion and the existing Profile pause remove it. Forced colors retain a visible selected state. Verify native/non-native isolation, all25 controls, deselection, Multi minimum, actual canonical launch destinations, Back/Forward/reload, busy cancellation, guest access, mask failure, keyboard, 320–430px and landscape. No new backend/storage operation is introduced.

### Open-paper live Quiz (web only)

- Owner-approved browser variant (2026-09-12): `html:not(.android-webview) .quiz-page` in `mystyle.css` uses one continuous white canvas, without a raised container or question card. Two uninterrupted1px section dividers use `--color-surface-strong`; no lateral border, outer radius or question shadow. Header/progress, the existing question scroller and the original web command bar retain their semantic order and actions.
- Outer space is10px above/16px below plus safe areas, with12–32px fluid side gutters and6px row gaps; short viewports use6px top and4px gaps. Question spacing is14px above/10px below,8px on short screens. Only signed Admin markers reserve an additional22px footer clearance. Reading height uses the existing viewport owner; there is no second scroll container or hidden content.
- Browser number styling retains its established status colors and type. The active number no longer scales into the scroller edge; its quiet shadow is `0 2px 5px` with18% of `--color-dark`. The row reserves10px vertical/8px horizontal space for the complete shadow and focus ring, with a visible thin scrollbar only when needed. The first and last questions follow the same geometry. These changes do not reach installed Android or other routes.

### Focused live Quiz (installed Android only)

- Owner correction (2026-09-12): every focused layout selector requires `html.android-webview`. Normal desktop/mobile browsers, including Facebook/Messenger, use the open-paper web variant above with their original typography, answer icons and emerald player/artwork. The static route class alone must never enable app presentation. Shared translation/data/accessibility behavior and the explicitly shared login remain unchanged.
- Installed phone translation adds a second neutral52px close target in the bottom-right, with a24px X and safe-area spacing. It occupies its own grid row outside the content scroller, uses the existing `data-help-close` owner, and has no animation or attention color. Top X, focus restoration and Android Back remain; tablet inline help and ordinary browser help have no bottom action.

- Named variant: `html.android-webview body.quiz-focus` in `mystyle.css`; audio stays owned by the same native-gated compact variant in `audio-player-ui.css`. Matte pearl canvas, white reading surface with one fine divider, graphite text/answers and existing blue selection. No new palette owner, business rule, or quiz engine.
- Header: section title only at22px, one48px frame for44px A-/A+ controls and fixed90px timer at21px. `assets/fonts/magicbook-latin-fonts.css` owns the shared Norwester face for login and Quiz display; use it for the timer (`20 : 00`),20px answer labels and16px circular navigation digits. All body/Bangla faces and reader-size controls remain unchanged.
- Compact reading density: navigation renders34px circles inside unchanged44px touch targets, with2px gaps and no shadows. The numbered row remains a scroller, never a10/20/30 timeline. The shared canvas uses8px horizontal padding and8px row gaps; the question surface uses10–20px padding,6px content gaps and8px question text padding, giving reading content more width and height. `quiz-help.css` owns10–16px panel padding,10px word/detail spacing,1.5 translation line-height and1.05–1.2rem default /1.32rem large /1.46rem extra-large translation scales. The close icon is18px without a visible circular backing, inside its unchanged44px accessible target. No question text, translation content, audio, grading or navigation behavior changes.
- Controls:44px slim player on the left, previous/next stacked at the right, text-only48px Vero/Falso and44px language targets with existing clover/scallop backings. Current question has an extra outline; answered circles fill blue, not a correctness signal. Geometry is independent of answer, loading and error states.
- Web keeps its original exit control. Android uses its existing WebView Back bridge plus a same-document history guard; the header arrow is hidden only after that guard is installed. Cancelling exit preserves the current answers. No APK or native permission change.
- Phone translation reader: a body-level white surface uses the shared viewport-height owner, safe-area padding, zero outer corners/shadow/blur and a56px minimum header with20px Norwester title. Its16px-gutter content has one vertical scroller; source question uses16px Arial (18.24/20.48px reader presets) beside the existing80–112px contained figure. The established compact Bangla scales remain. `mystyle.css` owns help/dialog layers900/1000; loading and offline notices remain above them. `quiz-help.css` owns the named full-screen presentation, scrollbar geometry and180ms content entrance; reduced motion removes the entrance. The desktop glass specimen and question/answer geometry do not change.
