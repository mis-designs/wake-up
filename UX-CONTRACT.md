# Magic Book learning insights UX contract

## Learning read recovery (web and installed Android)

- `src/learning-insights.js` owns the shared screen/dock request. An explicit `401 token_expired` uses the existing `script.js` `ensureAccessToken({ force: true })` validation and retries once; overlapping learning requests share renewal. Other authorization failures keep the existing re-login state, without retries or cached-data fallback. Navigation, account/device changes and cancellation reject stale results.
- Optional `MagicBookLearningSync` cache/outbox reads and cache writes have a 1.5-second UI wait budget. A stalled database does not prevent online statistics/errors from loading. No stored answers are deleted, no storage owner is replaced, and the existing user-scoped IndexedDB/synchronization remains unchanged. If local reads time out, online data may not yet include unsynchronized answers.
- Verify actual native chapter/dock taps, expired-token recovery, revoked/invalid access, stalled storage, offline cache, failure/retry and account/device cancellation. These are reproduced failure paths, not confirmation of the cause on an unconnected physical phone.

## Canonical UI map

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
| --- | --- | --- | --- | --- |
| Native Quiz/Exam selection | `android-mode-screens.js`, `android-mode-screens.css` | Existing `script.js` pill builders, trial guards, action gate and canonical quiz/PDF destinations | Android-only full-page `/scegli-quiz` / `/scegli-exam`, three tabs; browser sheets unchanged | All25 chapters, selection reset, Multi minimum2, guest guards, busy cancellation, history/reload, keyboard and narrow/landscape |
| Installed study Home/chapter shell | `android-study-shell.js`, `android-study-shell.css` | Existing `script.js` routing, selectedChapter and action gate | Owner-approved Android-only rotary variant; browser uses unchanged legacy UI | Native/browser gate, 320px reflow, actual route integration, reduced motion |
| Home tap gesture | `native-book-tap` in `android-study-shell.css`; browser geometry in `style.css` | Supplied `icons/clich_here.svg`, existing native hand | Native book5s; browser OPEN2.8s with shorter travel; decorative only | Timeline/color/press, unchanged button bounds and routing, keyboard/touch, asset failure, reduced motion, forced colors |
| Chapter rotary input | `android-rotary-model.mjs` + `android-study-shell.js` pointer lifecycle | `script.js` selectedChapter, bounds 01–25 | Finger-aligned drag, midpoint hysteresis, tap on numbered arc and keyboard; local selection accepts chapter entry while route launches stay gated; no wrapping or separate arrow buttons | Trusted touch, entry gesture, blur/cancel/capture failure, stale-pointer recovery, modal ownership, multipointer, boundary and selected-route tests |
| Study dock progress | `src/learning-insights.js` readProgress | Authenticated user/device-bound model; quizCoveragePct/catalogQuizCount | Loading, genuine zero, known percentage, saved/offline, error/unknown | No fabricated percentage; abort and session-change guards |
| Form | Login and dormant Promo access forms | `index.html` `#login .login-form`, `#promoAccessCard` + shared login helpers | Phone access; server-requested admin password; feature-switched promo conversion landing | Login behavior and browser keyboard/failure checks |
| Shared login presentation | `login-experience.css`, `login-experience.js`, `login-signs.mjs` | Canonical phone form/auth actions; existing public numberless figures and dictionary word meanings | Unibody on web/Android; book/sign top on mobile, split desktop; fixed sign frame, bounded cancellable motion, visible pause, natural keyboard-aware scrolling, small signature | Phone/admin/password states, asset failure/retry, stale decode, layout stability, keyboard resize/overlay, reduced motion, 320–1920px; physical keyboard follow-up |
| Scrollbar | Learning screen root | `src/learning-insights.css` | Browser forced-colors only | No nested horizontal scroller; no page overflow at 320px |
| Statistics workspace | `src/learning-insights.js` | Authenticated learning model | Empty, insufficient, ready, cached, refreshing | Data-state tests and 320–1920 browser verification |
| Error recovery | `src/learning-insights.js` | Authenticated errors and plan | Five categories; empty/populated master-detail; recovered section | URL, interaction, focus, and responsive verification |
| Shared controls | Learning JS/CSS + local daisyUI build | `d-btn` primitives with scoped geometry | Labelled refresh on wider screens; icon-only on narrow screens | Keyboard and 320/375/430/768/1024/1280/1440/1920 checks |
| Quiz correction translations | `quiz.js` + `quiz-help.js` data service | `mystyle.css` visual owner; synchronized help and catalog data | One exclusive accordion across all viewports; the active desktop panel may sit beside its row | Dedicated result/help tests plus keyboard and responsive browser verification |
| Live quiz bilingual help | `quiz-help.js`, `quiz-help.css`, `#quiz-help-workspace`; shared `quiz.js` dialog focus helper | Authenticated question and existing help resolver; DESIGN.md phone variant | Full white viewport reader on phones including landscape; one inline glass disclosure on tablets/desktop; source question + original figure, translation, word detail, keywords, context | Viewport bounds, focus/inert/scroll restoration, X/Escape/Android Back, resize, slow/stale/error help, figure failure, offline/timeout interruption, no duplicate figures or data requests |
| Live quiz question actions | `quiz.html` `.quiz-question-actions` | `mystyle.css` and `quiz-help.css` | Reserved wrapping footer after the question scroll region; optional explanation, Admin audio tools, and first-use help hint | Source-order, 44px target, long-text, reader-size, 320–430px, zoom/reflow, and iOS Safari checks |
| Admin quiz answer marker | `api/quiz.js`, `api/local-quiz-bank.mjs`, `quiz.js`, `mystyle.css` | Signed Admin role and private local answer bank | Green under Vero or red under Falso according to the correct value | Authorization, public-payload, client-visibility, and contrast tests |
| Quiz timer | `quiz.js`, `#timer`, `#custom-modal` | Signed Admin role returned by `api/quiz.js` plus the active quiz duration | Learner countdown with continuation dialog; Admin elapsed overtime | Boundary, authorization, dialog focus, repeated continuation, elapsed-time, and cache-version tests |
| Global connectivity alert | `offline-notice.js`, `offline-notice.css` | `navigator.onLine` plus browser `online` and `offline` events | Initial offline; connection lost; automatic recovery | Shared static test plus browser offline/online verification |
| Loading indicator | `loading-ui.css`, `icons/loading.gif` | Shared async-state contract | Page, panel, inline status, busy control; static reduced-motion fallback | Asset/cache, busy-state, layout-stability, and reduced-motion tests |
| Quiz figure presentation | `api/quiz-figure-image.mjs` through `api/asset.js` | Every `kind=figure` response is sanitized in source pixels before delivery | Quiz live/correction, Studia quiz, Statistiche/Errori, and Admin explanation tools; each keeps its established frame and fallback | Pixel-boundary tests, fail-closed test, client-route inventory, fresh cache key, narrow viewport, resize, and 200% zoom checks |
| Explanation audio player | `audio-focus.js`, `audio-player-ui.css` + owning playback JS | Shared Quiz/Studia quiz/Admin explanation transparent emerald pill using only the approved play/pause SVG pair; Admin legacy review uses the same styles, never its former black player | Idle, playing, TTS-interrupted, loading, unavailable/error; 1x to first-click 0.5x, then up to 2x | No overlap, latest-action race tests, timestamp-preserving resume, manual-stop suppression, bounded 320px geometry, ARIA label, keyboard, reduced-motion, forced-colors, and cache tests |
| WhatsApp group dialog | `script.js`, `style.css` | Authenticated Home invitation state | Bangla default; Italian alternate; join or defer | Focus trap/restoration, locale, responsive, reduced-motion, and cache tests |
| Android WebView density | `mobile-experience.css`, `script.js`, Android `MainActivity.kt` | Standard Android WebView marker plus live utility-count state | Compact, vertically balanced Home; centered collision-free header; fluid chapter selector and Quiz; touch-safe Admin scrolling; ordinary browser layout remains unchanged | Native marker, utility rails, auto-margin overflow, Admin scroll ownership, touch target, drag-frame, cancellation, keyboard alternative, exclusive-action, and Android build checks |
| Android installed palette | `android-webview-mode.js`, `android-app-theme.css` | `html.android-webview` and compatibility `data-app-palette="aura-fluid"` | Pearl, graphite, neutral gray and blue roles from DESIGN.md; thinner section-only headers; browser styles and semantic status colors unchanged | Early-marker, route coverage, offline cache, contrast, header geometry, browser regression |
| Chapter title/artwork | `android-study-shell.js`, `android-study-shell.css` | Selected chapter + existing All Books cover manifest | Fixed left frame: whole-word local fitting, no split final letters; title first for at least1.8s, then complete square-cornered artwork; failures/explicit pause keep title | All25 titles including per-word line bounds and fallback font; full images, stale/failed image, pause, resize and route cancellation |
| Native bilingual action labels | `android-study-shell.js`, `android-study-shell.css` | Existing six action buttons, owner-supplied icons and exact Bangla strings | Matte buttons and solid scalloped icon backing without glass or outer ring; icon and maximum bilingual label width are centered together, each line centered in the fixed40px rail; white SVGs, original-color PNGs; static bilingual reduced-motion/pause; visual-only pointer contact recovers on scroll/cancel/blur; launches reuse the same action gate | Icon assets/cache, no movement during language changes, six translations, font,320px fit, press/release, keyboard focus, motion preferences and duplicate-route rejection |
| Native traffic divider motion | `android-study-shell.css` | Existing supplied blue roundabout SVG | Variable clockwise/counterclockwise and occasional double turns; lines stay still | Sampled angular travel, reduced motion and explicit pause |
| Native section icons | `android-app-theme.css`, existing `script.js`/`quiz.js`/`src/learning-insights.js` header owners | Same six assets as native chapter actions; active Quiz/Exam mode | Decorative title masks for SVGs; existing image slots for PNGs and Exam PDF; original browser assets unchanged | Six mappings, native/browser gate, narrow header geometry, Quiz/Exam switching, offline assets and missing-image stability |
| Native chapter spacing | `android-study-shell.css`, inert `#androidStudyTemplate` | Existing chapter selector, six actions and dock | One bounded selection/action group; equal48px (compact44px) targets; start-aligned labels beside stationary icons; quieter divider | Stable25-title geometry, complete cards, Latin/Bangla fit,320px portrait and short-landscape scroll above dock |

## Feature ownership

| Concern | Owner | Contract |
| --- | --- | --- |
| Native study navigation | Existing route actions through `android-study-shell.js` | Book to chapters; Vai and the currently presented image card open the selected book through the same exclusive gate. Hidden, stale, loading or failed artwork cannot launch. Four requested study controls and statistics/errors reuse canonical routes. Profile/Admin elements are moved with listeners and access checks intact. Admin stays unavailable to non-admins. |
| Native rotary feedback | Android `MagicBookHaptics.kt` + pure `HapticPolicy.kt` | Exact trusted HTTPS main frame only; two fixed command names, no user-supplied timing. Device settings respected; boundary is two ticks. No haptics available is not an interaction failure. |
| Native study assets | `assets/native-chapter-covers.json`, `assets/chapter-covers/README.md`, supplied book/divider SVGs | All Books public cards map explicitly to chapters01–25 as local WebP copies. The complete original card, including its footer artwork, fits proportionately in the square-cornered left-hand frame. The semantic Vai button lives inside the right wheel and shares the icon backings' scalloped silhouette; its white label stays readable if the decorative asset fails, with unmasked focus and touch geometry. Loads are selection-versioned; failures preserve geometry and the real title. Book SVG receives no extra shadow. |
| Learning shell | `src/learning-insights.js`, `src/learning-insights.css`, `src/daisyui.css` | Back, refresh, routes, primary/secondary actions, category tabs, and pagination use local `d-`-prefixed daisyUI. Legacy fixed chrome is hidden while the workspace is active. |
| Learning routes | `script.js` | `/statistiche` and `/errori`; the Errori lens is stored in `tipo`. |
| Learning data | `src/learning-insights.js` | User-scoped cached rendering plus one abortable request per visible route; stale requests cannot replace newer state. |
| Server aggregation | `api/learning-insights.mjs` | Authenticated, device-bound, no-store response; server grading and catalog mapping remain authoritative. |
| Chapter disclosure | `src/learning-insights.js` | One selected matrix cell; detail appears beside the matrix on wide screens, sequentially on tablet, and as a closable bottom sheet on phones. Trigger uses `aria-expanded` and `aria-controls`. |
| Error master/detail | `src/learning-insights.js` | At most one selected item per category. The master trigger controls the matching detail and focus remains recoverable after rerender. |
| Pagination | `src/learning-insights.js` | Eight initial items; `Mostra altri` appends without losing stable focus. |
| Async status | `#learningInsightsStatus` | Polite live region for refresh, offline cache, and retry results. |
| Shared loading feedback | `loading-ui.css`, `icons/loading.gif` | User-visible initial loads, panel requests, and button operations use the same GIF with truthful Italian status copy and `aria-busy`. Busy mutations disable duplicate activation without changing control dimensions. Background refresh preserves current content and uses its existing subtle status instead of a blocking overlay. Reduced-motion replaces the animated image with a static system-operable mark. |
| Audio focus | `audio-focus.js`, `quiz.js`, `quiz-help.js`, `study-quiz.js`, `magic-dictionary.js` | Explanation audio is the resumable source; question, keyword, and dictionary TTS are transient sources. A transient source pauses the active explanation without resetting `currentTime`. Superseding TTS owns the same interruption and only its natural completion may resume. Manual TTS stop, route/context teardown, or a direct explanation request suppresses automatic resume. Pending explanation requests are invalidated so a late response cannot overlap newer audio. |
| Quiz blocking loading | `quiz.html`, `quiz.js`, `mystyle.css` | Initial quiz load and result checking use a seamless white full-viewport state with only the centered shared GIF visible. Status text remains available to assistive technology. Failure closes the loader and presents plain Italian recovery copy without backend details. |
| WhatsApp group invitation | `script.js`, `style.css`, local Bengali font assets | Home may show one modal invitation after authentication and outside other blocking overlays. Bangla is the default; the learner can switch to Italian, join, defer for seven days, close with Escape/backdrop, and receive trigger-focus restoration. Background content is inert while the dialog is open. |
| UI assets | Repository images/SVGs + authenticated figure endpoint | No Unicode glyphs as UI icons. Decorative CSS marks remain hidden from assistive technology. |
| Magic Styles | `libreria-font.html`, `libreria-font.css`, `magic-styles.css`, `loading-ui.css`, `assets/fonts/magicbook-bangla-fonts.css` | Admin keeps the stable `/libreria-font` route with the label Magic Styles. The library presents live shared components, explicitly labelled preview progress, real destination links, and the three original Bengali font specimens. The Exam 80 badge is decorative; its whole card remains one keyboard-accessible action. Motion preference and forced colors are respected. |
| Promo conversion | `#promoAccessNextStep` + `openPromoPackages()` | A previous promo or a full campaign produces persistent inline guidance; the user chooses when to open `/join`, which focuses the packages heading. |
| Promo login availability | `PROMO_LOGIN_ENABLED` in `script.js` | The default `false` state keeps `#promoAccessCard` hidden before paint, skips promo setup/status requests, rejects promo-card activation, and routes logged-out welcome traffic to `/login`. Setting only the switch to `true` restores the preserved promo flow. |
| Quiz correction translations | `quiz.js`, `quiz-help.js`, `mystyle.css` | Each correction row owns one on-demand translation disclosure. `quiz-help.js` owns data resolution and caching; `quiz.js` owns row state and interaction; `mystyle.css` owns responsive presentation. |
| Quiz correction audio | `quiz.js`, `mystyle.css`, `audio-player-ui.css`, `icons/explain_quiz.svg` | Every correct, wrong, and unanswered row exposes the same labelled person-artwork audio control. Audio is fetched only after activation, only one review audio plays at a time, and missing audio uses the established unavailable message. The shared player keeps centred play/pause geometry and updates its accessible action label. |
| Live quiz bilingual help | `quiz-help.js`, `quiz-help.css`, `quiz.js` shared dialog focus owner | One on-demand workspace: full white reader on phones with official question and existing figure, inline glass on tablets/desktop. X/Escape/Android Back, focus/inert/scroll restoration, stale cancellation and responsive transfer preserve quiz state and existing data/audio owners. |
| Live quiz question layout | `quiz.html`, `mystyle.css`, `quiz-help.css` | The figure and Italian question precede the optional Admin recorder in one bounded scroll region, followed by a reserved wrapping action footer. “Spiega” and the first-use help hint remain in normal flow, never cover question copy, and wrap to another footer line when their combined width does not fit. Reader-size and browser zoom remain supported. |
| Admin correct-answer cue | `api/quiz.js`, `api/local-quiz-bank.mjs`, `quiz.js`, `mystyle.css` | The server attaches `admin_correct_answer` only after signed Admin authorization. The client renders exactly one low marker under the matching Vero/Falso control, exposes a text equivalent, and leaves no marker space for ordinary users. |
| Admin quiz overtime | `api/quiz.js`, `quiz.js`, `#timer` | Only the server-returned signed Admin role enables uninterrupted overtime. At the normal limit the display changes from `0:00` to `+0:00`, counts upward, and never opens the learner timeout dialog. Manual Admin completion reports the full elapsed time. |
| Learner timer expiry | `quiz.js`, `quiz.html` `#custom-modal`, `mystyle.css` | At the active duration limit, a non-Admin quiz pauses at `0:00` and opens one `alertdialog`. `Sì, continua` grants another full cycle while preserving total elapsed time; `Chiudi quiz`, Escape, or backdrop dismissal exits without grading. The choice repeats after each completed cycle, stops active audio, closes transient help, and waits safely if another shared dialog is already open. |
| Study explanation audio | `study-quiz.js`, `study-quiz.css`, `audio-player-ui.css`, `icons/explain_quiz.svg` | Each available explanation keeps the supplied artwork beside the shared player. The artwork is decorative, reserves stable geometry, mirrors play/pause state, and shrinks without displacing the controls on narrow screens. |
| System figure presentation | `api/quiz-figure-image.mjs`, `api/asset.js` | Every dynamic quiz figure removes only isolated, neutral digit-sized components on the upper-left label line. A fixed rectangular cover is forbidden. Lossless PNG preserves pixels outside the detected digits, and uncertain labels leave the figure intact. This is independent of CSS geometry, screen size, orientation, browser zoom, text zoom, and `object-fit`; no surface may recreate a local overlay mask. Original R2 files and separately rendered figure metadata remain unchanged. Processing errors fail closed, while each consumer keeps its existing missing-image recovery. Current clients include the `numberless-v2` presentation key to bypass pre-change CDN entries. |
| Admin user loading | `script.js`, `api/admin.js` | Opening Admin requests only the 10 newest users. Phone search uses the authenticated server action after a 300 ms debounce and ignores stale results. The complete list is requested only through `Carica tutti gli utenti`; Promo and Duplicati explain that they require that complete scope. Dataset filters expose tab state and support Left/Right/Home/End keyboard movement. |
| Global connectivity alert | `offline-notice.js`, `offline-notice.css` | Every HTML entry loads one shared alertdialog. It blocks interaction only while `navigator.onLine` is false, keeps the background inert, restores prior focus on reconnect, and never uses browser-native alerts. |
| Android native shell | `mobile-experience.css`, `script.js` + Android `MainActivity.kt` | The shell fixes browser text zoom at 100 percent and marks its document as `android-webview`. Home omits the unreliable remote promo frame; its action cluster centers when it fits and becomes a top-safe natural scroll when it does not. The Admin route owns one full-height vertical scroller with native touch momentum, while the body remains fixed and ordinary browsers retain document scrolling. The header derives symmetric side rails from visible Admin, Profile, and menu utilities so the title never sits beneath an icon. Essential actions retain usable targets and one transition owner rejects competing launch taps. The chapter carousel batches pointer work once per frame, adds resistance only at its real boundaries, projects a bounded release, and snaps without launching after cancellation. Cards are semantic buttons with roving focus; tap and Arrow/Home/End remain non-drag alternatives. |
| Android app-only theme | `android-webview-mode.js`, `android-app-theme.css` | The synchronous marker activates the pearl/graphite/blue palette only in Android WebView. Its aura-fluid value remains a drag compatibility switch. Theme-color reads the primary token. Shared thin section-only headers omit duplicate Magic Book branding; route content, access rules and ordinary-browser geometry stay unchanged. Semantic correct/incorrect/warning/failure colors remain independent. |

## Installed Android mode navigation

- Quiz/Exam selection is a full-page region, not a modal bottom sheet. The existing content cards become one visible tabpanel at a time; hidden panels and the underlying chapter/dock controls cannot receive input. `script.js` remains the only owner of eligibility, trial attempts, chosen chapter values and quiz/PDF launches.
- Quiz-only thumb-zone variant: the mode tablist and original Start nodes live in one footer below the content scroller; only the active panel's Start is visible/focusable. No cloned launch handlers. The shared Mix action has the stable `qmsMixStartBtn` ID, also used by guest-attempt decoration. Exam and ordinary-browser sheets keep their previous layout. Italian/Bangla explanations reserve their larger natural height, keep accessible language-tagged static equivalents, and show both with reduced motion; trial restrictions never rotate away. The decorative30 remains the actual fixed Mix count.
- Android entry sets the canonical route/title and focuses its heading. Back/Escape uses owned history where available, otherwise the chapter route; Back/Forward clears pending launch feedback before restoring a screen. Browser-only flow and its duplicate Study entry are intentionally preserved for compatibility; Android uses its existing dedicated Study action.
- The arch/soft-clover number controls retain stable target bounds, native button semantics, `aria-pressed` and explicit selection copy. Tabs preserve each selector's current UI until the canonical selection reset or route re-entry. Selecting a chapter still clears Multi and vice versa. Start remains disabled until the existing minimum is met; a launch retains a stable busy screen and suppresses competing controls.
- Scope is presentation/navigation only: no questions, scoring, timers, access checks, statistics, IndexedDB or synchronization policy is changed. Sources: existing `script.js` `_buildQMSCapPills`, `_buildQMSMultiPills`, `startMixQuiz`, `startCapQuiz`, `startMultiQuiz`, `openExam`, `startExamQuiz`, `startExamPdf` and exclusive action gate.
- A document restored from the browser page cache reopens its canonical selector and clears stale busy controls. An already validated in-document guest trial may return to `/prova-gratis` or its Quiz selector through history; a URL alone never grants trial credentials or opens Exam. The existing trial service still validates entry and its original chapter restrictions remain.

## Statistiche

- The first viewport answers `Come stai andando?` with real recent/cumulative results and one immediate action.
- The summary reports observed totals only: correct/total answers, distinct quizzes, chapters started, active review items, recent-window accuracy, and recovered count.
- Comparison copy uses simple thresholds (`stai migliorando`, `più errori del solito`, `stabile`) and never invents an answer sequence.
- Empty history offers the first quiz. Insufficient history states how many answers are still useful. Ready history uses the first real plan item or a positive continue action.
- Two compact groups show up to three chapters going well and three to review.
- All 25 chapters appear in one matrix. Each cell contains number, title, explicit simple status, and optional review count.
- Chapter detail retains existing quiz and book destinations and reports attempts, quiz coverage, correct answers, recent result, active review items, and recoveries.

## Errori

- The heading is plain `Errori` and summarizes items to review, improving chapters, and recovered items.
- `Da controllare` contains at most three real active items and opens the matching category/detail.
- Figure, Quiz, Parole, Argomenti, and Capitoli form a compact five-tab grid with visible counts.
- `Capitoli` is derived client-side from `model.chapters`; it does not extend the API.
- Recovered items are not a tab. They remain visible in a separate positive section capped at three.
- Detail uses count-based observational copy. It never exposes backend causal/diagnostic phrasing.
- Figure items load the real authenticated numberless media lazily; word items keep the dictionary action; related quiz and chapter actions reuse existing destinations.
- `Il tuo ripasso` contains at most three actions, shows total minutes, and moves before category exploration on mobile.

## Correzione quiz

- Every correction row can reveal the full Bangla translation of the Italian question, exactly two Italian context tags for chapter and topic, and keyword pairs with Italian and Bangla labels.
- On the learner-facing Studia quiz page, keyword pairs and their optional detail appear before chapter/topic tags. The same reading order is preserved in correction surfaces. Both context tags are uppercase; Italian words, locutions, and explanatory copy begin with an uppercase letter unless the source is an intentional special form.
- Keyword selection is owned by the shared All Books glossary resolver: isolated articles, prepositions, conjunctions, and auxiliary forms never render as teaching chips, while the same tokens remain valid inside a useful multi-word technical phrase. Phrase components are absorbed unless the central glossary explicitly marks a words-only exception. Magicph applies the same policy again at both V3 and V2 rendering boundaries.
- Help resolves in this order: synchronized V3 data, catalog translation, then the protected automatic fallback only when no usable Bangla translation exists. Contextual Bangla copy is never substituted for the full question translation.
- The shared static catalog may prewarm once while the browser is idle. Per-question resolution and any protected automatic fallback start only after the learner opens that row; opening the correction must not fan out help requests across all 30 rows. Resolved and in-flight work is cached or deduplicated, and stale results cannot update a closed or different row.
- Opening one translation closes the previously expanded row on every viewport. The active desktop panel may sit beside its row when width permits without changing semantic reading order.
- The disclosure stays inside its correction row, preserves stable media and action geometry, and never hides the fixed result actions.
- Correct, wrong, and unanswered rows all keep the explanation-audio button visible. Activating it lazily requests that question's signed audio; changing row or closing the result stops the previous explanation, and unavailable audio is reported without removing the correction content.

## Aiuto bilingue durante il quiz

- Translation and keywords use one workspace: phones display a white full-viewport modal reader; tablets and larger screens keep the glass panel inside the existing question scroller. The fullscreen reader shows the official Italian question with its existing figure beside it, if any; quiz navigation/player/answers remain underneath and inert. The header/X stays reachable while the body scrolls.
- The full Bangla translation is followed by the expanded word detail, then real Italian keyword text buttons. Each button opens its Bangla meaning, explanations and audio above the keyword list; a second activation closes the detail. Reveal it within the active reading scroller without moving keyboard focus.
- The question button, including spare blank space, exposes aria-expanded and opens help explicitly. On phones, focus enters the labelled dialog and Tab/Shift+Tab use the shared quiz dialog trap, including context summaries; the original quiz scroll and figure return on close. Inline mode keeps normal document focus order. Escape/X and Android Back close either presentation without changing answers or timer rules. Resize transfers the same visible content between modes without refetching. Blocking dialogs/loaders close help first; the offline alert remains above it and keeps its own isolation until reconnection. Closing and every question change invalidate pending help/word audio, and a new question starts closed.
- Only a signed Admin quiz response may contain the correct answer. The low green/red marker is absent for learners and cannot be inferred from empty reserved layout space.
- Only a signed Admin quiz response may enter uninterrupted overtime. At zero the timer exposes a textual `Tempo supplementare Admin` state and counts upward from `+0 : 00`. All other roles pause at `0 : 00` and choose whether to begin another full cycle or close the quiz without grading.

## Responsive behavior

- Browser Home OPEN reuses the supplied Android hand and shared CSS tap timeline, with a2.8s browser cadence. The absolute decorative cue cannot intercept pointer/keyboard input, resize the action or change `showChapters()` routing. It hides during interaction, unavailable/launch states, reduced motion and forced colors; leaving Home removes it with the existing route. Native Home keeps its original5s gesture.
- Installed rotary Home has no tap hint or visible pause button. Its locally persisted animation control is in the existing Profile panel, only while this app-only shell is active. Reduced-motion remains static.
- Installed Home greetings use a fixed44px rail: `আসসালামু আলাইকুম` in Hadi Rounded with lang=bn, local-time greeting and Ciao in Norwester. Shared web/Android login now uses the distinct owner-approved unibody sign composition: no greeting rail or large brand heading. The phone label and login heading remain accessible, with authentication unchanged. The hand repeats every5s, black below the book and white over it. Bent arrows fade along mirrored arcs every3.8s; they hide during rotation and return after4.5s idle. These supplied SVGs are decorative, non-interactive and motion-safe. A stationary tap on a numbered arc remains the pointer alternative to dragging; cancellation never launches.
- Installed chapter selection sizes its transparent right arc from space remaining after header and six controls. Its sole scroller ends12px above the dock. All25 titles preserve positions at320×568 and larger portrait sizes; landscape/enlarged content scrolls above the dock. Resize cancels dragging and Back restores selection with one route owner.
- The selected number and its small circumference marker are blue, with no black wheel, badge or duplicate readout. Both use the same angle; the marker stays exactly on the circle during drag and settlement. Solid-blue Vai, with white text, sits entirely inside the circle and outside the slider's semantic subtree. A fixed left frame shows the El Messiri title (28–16px local fitting), then complete decoded artwork after at least1.8s with a soft fade. The real title stays accessible and handles missing/slow artwork. Changing chapter, hiding or dragging invalidates old reveals. Reduced motion removes transitions; explicit pause keeps the title.
- Six main action labels move upward inside fixed40px windows: original for2.76s, .552s transition, Bangla for3.312s, then upward to original. Exact Bangla strings and fonts are documented in DESIGN.md. Accessible names include both languages, never repeated animated announcements. Profile pause/reduced motion shows both labels statically without resizing. Background/navigation pauses rails; hover and keyboard focus pause reading. The divider's blue roundabout alternates1/1/2/1 eased turns and direction with idle intervals over40s; the road lines remain still.
- Each changed chapter requests one AndroidX selection tick; an attempt before1 or after25 requests two boundary ticks85ms apart instead, never a third combined tick. Device/system haptic settings are respected. The origin-restricted native bridge and installed shell version determine availability; browser emulation does not verify physical vibration.

- Public access: while Promo Code is disabled, logged-out welcome traffic opens the personal login directly. When promo access is enabled, from 1024px its pass uses two columns — campaign title and timer on the left, labelled phone/code form on the right — with Login/Join/About in one compact row below; narrower viewports retain the semantic vertical order.
- 1280–1920: wide grouped surfaces use the viewport; Statistics is overview/action, chapter matrix/detail; Errori is explorer/plan with master-detail.
- 768–1024: major regions stack only when necessary; chapter detail becomes sequential; no control is compressed below a usable target.
- 320–430: one vertical flow, two-column chapter matrix, three-column category grid wrapping to two rows, sequential detail, plan before categories.
- Quiz correction: translations use one exclusive accordion on every viewport. From 768px, the single active panel may use the adjacent column when space permits. Italian tags, Bangla copy, and keyword pairs wrap without clipping or page-level horizontal overflow.
- Live Quiz: at every width, long or enlarged Italian copy scrolls in its own card region above the question-action footer. The reserved footer remains visible, wraps at narrow widths, and never floats over the text.
- Reading order is valid without CSS. Italian and Bangla copy wraps, actions remain visible, and page-level horizontal scrolling is forbidden.
- In Android WebView, the Home action cluster is vertically centered only while its measured content fits. At shorter heights, auto margins collapse and the existing vertical scroller owns overflow. The fixed Admin/Profile utilities and in-header menu receive explicit right-edge slots, while equal left/right header rails keep the route title centered.

## Navigation, async, and failure behavior

- Home, chapter, Exam, Quiz-mode, Admin, and delayed route launches share one transition owner. A second activation during that interval is ignored; delayed callbacks verify ownership before navigation, and browser Back/Forward cancels stale pending work. Trial counters mutate only after their launch action acquires ownership.
- Home entries and the Statistics/Errori switcher push real routes. Lens selection replaces only `tipo`; invalid values fall back to `figure` and normalize the URL.
- The Admin utility row links to `/libreria-font`. The font library has its own localized document title, a real back link to `/admin`, a clean static route, and a dedicated offline fallback page.
- Admin entry, refresh, search clearing, and successful mutations preserve an explicit data scope: recent, searched, or complete. Promo metadata is lazy and is never fetched during the initial recent-user load.
- Initial load renders a skeleton matching the overview/action/matrix geometry.
- Valid cached data appears immediately with its update time while a background refresh runs online.
- Pending local answers are sent only to the authenticated Vercel endpoint, validated, and merged by event ID.
- Offline with cache keeps the screen usable; offline without cache, expired access, 14-second timeout, and generic failure have separate recovery copy.
- A global connection-loss alert appears immediately when the app opens offline or loses connectivity. It remains stable without repeated toasts, disappears automatically on `online`, and restores the prior focus. The cached page remains rendered underneath and is revalidated by its owning workflow after reconnection.
- Retry is idempotent and disabled while a request is active. No backend implementation detail is shown to the learner.

## Accessibility

- Route entry focuses the screen heading; background refresh does not steal focus.
- Category tabs implement Left/Right/Home/End and link selected tab to its tabpanel.
- Expanded chapter/error controls expose `aria-expanded` and `aria-controls`; rerender restores focus to a stable trigger or pane.
- Each quiz-correction translation uses a native button with `aria-expanded` and `aria-controls`; opening or closing it does not move focus, and its touch target is at least 44px. Bangla content declares `lang="bn"` and remains readable at zoom.
- The live bilingual-help text pills and close button have at least 44px targets. The panel uses normal document keyboard order and preserves the surrounding quiz controls.
- Status is always textual as well as colored. Icon-only controls have accessible names; decorative images use empty alternatives.
- Reduced-motion removes spin/pulse duration, forced-colors preserves selected state, and touch targets remain at least 40–44px.

## Admin explanation catalog

- The existing chapter-button and inline recorder owners in `aggiungi-spiegazioni.js` also own Exam 80, placed after the 25 chapters with `?exam=80` navigation and the same search, filter, figure, draft, playback, and save controls.
- `api/quiz-audio-catalog.mjs` resolves all 868 questions on the server. Sharing requires identical Italian text (preserving punctuation and case), canonical figure and correct answer. Chapter location and row ID are not content. Unverified Exam content and conflicting normalized keys use isolated identities; ambiguous legacy audio never becomes playable merely because a request is signed. The Admin catalog supplies the same identities used by upload, deletion and playback.
- Existing chapter-only catalog consumers still receive 788 questions. Only the authenticated Admin audio catalog includes the 80 Exam rows and private answer values.

### Focused live Quiz (shared web / Android)

- Live Quiz and Exam share one compact page on Android and the public website. Dedicated mode selectors remain unchanged. Question, figure, translation, audio-focus, timer, grading and signed Admin policies keep their canonical owners.
- Android system Back closes the translation reader (fullscreen or inline) or the current dismissible layer before invoking the existing quiz-exit confirmation. The offline alert, loading and noncancellable messages are not bypassed. Ordinary browsers keep a44px back button and unchanged browser history; no native guard is installed there.
- The compact command area reserves geometry for unavailable/pending audio. Number controls expose answered/unanswered labels and aria-current; Vero/Falso expose aria-pressed, with correct-answer markers still restricted to signed Admin data. No answer correctness is inferred from blue selection.
- Rollout is the shared `/quiz` document and current PWA cache191; deploy both through the existing site pipeline. The installed wrapper loads that same document. Rollback reverts the focused-Quiz changes and ships a newer cache epoch, not a second implementation. Physical Android Back and authenticated production audio require device/release smoke testing.
